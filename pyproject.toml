[project]
name = "air-quality-detection"
version = "1.0.0"
description = "Air Quality Detection and Prediction System"
requires-python = ">=3.8"
dependencies = []

[tool.vercel]
entrypoint = "web_server:app"
